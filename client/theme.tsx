import { DefaultTheme } from "react-native-paper";

const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: "#A36361",
    accent: "#735751",
    background: "#FAF8F4",
  },
};

export default theme;
